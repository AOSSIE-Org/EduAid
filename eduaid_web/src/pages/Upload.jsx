import React from 'react';
import Text_Input from './Text_Input';

const Upload = () => {
  // This page reuses the existing Text_Input component which handles file uploads
  return <Text_Input />;
};

export default Upload;
